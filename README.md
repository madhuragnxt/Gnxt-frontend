
  # GNXT Transport

  This is a code bundle for GNXT Transport. The original project is available at https://www.figma.com/design/Q3vEEKUwC9FGTJNJO6ftPG/GNXT-Transport.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  